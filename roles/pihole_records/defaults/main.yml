---
# These defaults suit a native Pi-hole install, which is failhole. homeboi runs
# Pi-hole in Docker against a bind mount and overrides both values.
pihole_dnsmasq_dir: /etc/dnsmasq.d
pihole_reload_cmd: systemctl restart pihole-FTL
