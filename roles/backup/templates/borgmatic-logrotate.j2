# borgmatic does not rotate its own log_file. Without this config the file grows
# without a limit.
{{ borgmatic_log_file }} {
    weekly
    rotate 4
    compress
    delaycompress
    missingok
    notifempty
    copytruncate
}
